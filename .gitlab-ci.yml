stages:
  - build

build_pdf:
  stage: build
  image: texlive/texlive:latest
  script:
    - mkdir -p output
    - latexmk -pdf -outdir=output ./template/thesis.tex
  artifacts:
    paths:
      - output/thesis.pdf
      - output/thesis.log
    expire_in: 1 week
