stages:
  - build

build_pdf:
  stage: build
  image: texlive/texlive:latest
  script:
    - mkdir -p output
    - latexmk -pdf -output-directory=output ./template/thesis.tex
  artifacts:
    paths:
      - output/thesis.pdf
    expire_in: 1 week
