namespace GdP23.S10.A4.Template

module Lists =
    open Mini
    open Types

    // a)
    let isEmpty<'a> (l: MList<'a>): Bool =
        failwith "TODO"

    // b)
    let appendFront<'a> (v: 'a) (l: MList<'a>): Unit =
        failwith "TODO"

    // c)
    let appendBack<'a> (v: 'a) (l: MList<'a>): Unit =
        failwith "TODO"

    // d)
    let get<'a> (index: Nat) (l: MList<'a>): Option<'a> =
        failwith "TODO"

    // e)
    let update<'a> (index: Nat) (v: 'a) (l: MList<'a>): Unit =
        failwith "TODO"

    // f) (freiwillig)
    // Der Code muss hier stehen bleiben, auch wenn Sie die Aufgabe nicht bearbeiten!
    // Ansonsten gibt es im ExClaim-System Compile-Fehler mit den Testfällen.
    let remove<'a> (index: Nat) (l: MList<'a>): Unit =
        failwith "TODO"

