namespace GdP23.S11.A2.Template

module ArrayMap =
    open Mini
    open Types

    // a)
    let map<'a,'b> (f: 'a -> 'b) (ar: Array<'a>) : Array<'b> =
        failwith "TODO"

    // b)
    let inplaceMap<'a> (f: 'a -> 'a) (ar: Array<'a>) : Unit =
        failwith "TODO"

