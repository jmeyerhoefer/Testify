%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% LISTINGS %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\usepackage{comment}
\begin{comment}
\usepackage{listings}
\newcommand{\dollar}{\mbox{\textdollar}}
\newcommand{\hlcolor}[2]{\sethlcolor{#1}\hl{#2}}

\lstdefinestyle{FSharp}%
{
	% keywords
	morekeywords={
		let, new, match, with, rec, open, module, namespace, type, of, member,
		and, for, while, true, false, in, do, begin, end, fun, function, return, yield, option,
		try, mutable, if, then, else, cloud, async, static, use, abstract, interface, inherit, finally,
		Nat, int, float, double, bool, string, char, unit, list, array, set, seq,
		List, Array, String, Set, Seq, Some, None,
		RegExp, Eps, Lit, Cat, Empty, Or, Star,
		TestMethod, Timeout, Config, QuickThrowOnFailure, AreEqual, Check, One,
		Assertify, (?)
	},
	otherkeywords={let!, return!, do!, yield!, use!, var, from, select, where, order, by},
	alsoother={`},
	% colors
	basicstyle          = \scriptsize\ttfamily,
	backgroundcolor     = \color{colorBackground},
	keywordstyle        = \color{colorKeyword},
	commentstyle        = \color{colorComment},
	identifierstyle     = \color{colorIdentifier},
	stringstyle         = \color{colorString},
	morecomment         = [l][\color{colorComment}]{///},
	morecomment         = [l][\color{colorComment}]{//},
	% strings, delimiters
	morestring          = [b]",
	moredelim           = [s][\color{colorDelimiter}]{<@}{@>},
	moredelim           = [s][\color{colorMethod}]{``}{``},
	moredelim           = [s][\color{colorAttribute}]{[<}{>]},
	literate            =%
		{(?)}{{(\textcolor{colorMethod}{?})}}1
		% {->}{{$\to$}}1
		{<=}{{$\leq$}}1
		{>=}{{$\geq$}}1
		{|>}{$\vartriangleright$}1
		{<|}{{$\vartriangleleft$}}1,
	% range
	rangeprefix         = ////, % [1]
	rangesuffix         = ),
	includerangemarker  = false, % [2]
	% line numbering
	numbers             = left,
	numbersep           = 5pt,
	numberstyle         = \tiny\color{colorNumbering},
	% text
	showstringspaces    = false,
	keepspaces          = false,
	showspaces          = false,
	showtabs            = false,
	breakatwhitespace   = false,
	breaklines          = true,
	mathescape          = true,
	escapeinside        = {(*@}{@*)},
	tabsize             = 4,
	sensitive           = true,
	extendedchars       = true,
	captionpos          = b,
	% other
	aboveskip           = \bigskipamount,
	xleftmargin         = \parindent,
	framexleftmargin    = 0em
}

\lstdefinestyle{OutputX}%
{
	basicstyle			= \scriptsize\ttfamily,
	keywords			= {},
}

\lstset{style=FSharp}
\end{comment}